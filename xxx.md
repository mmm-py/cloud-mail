# 专群命令使用说明

以下命令均在**已授权的专群**内使用；未标注的默认为**仅超管**可用。

---

## 一、授权与基础

| 命令 | 说明 |
|------|------|
| **授权专群** | 把当前群标记为专群，自动生成 3–4 位编号，群名改为【等待交易】 专群 编号。 |
| **给我管理** | 给发令者完整管理员权限，头衔为「欧易群管机器人 官方员工」。（公群/专群均可） |
| **修改群名** 新的群标题 | 手动修改本专群群名，并同步到数据库。 |

---

## 二、押金与状态

| 命令 | 说明 |
|------|------|
| **收到供方押金** 1000 | 累加供方押金，自动算总押金并更新群名。若当前为「交易完成」会改回「等待交易」。 |
| **收到需方押金** 500 | 累加需方押金，同上。 |
| **清空供方押金** | 清空供方押金，重新计算总押金与群名。 |
| **清空需方押金** | 清空需方押金，同上。 |
| **开始交易** | 状态改为「交易中」，群名前缀变为【交易中】，发 hy.mp4 + 提示。 |
| **交易完成** | 清空押金与规则，状态改为「交易完成」，群名恢复【等待交易】 专群 编号；清空本专群所有担保单；撤销临时链接；发 hy.mp4。 |

---

## 三、担保单（开启担保后）

| 命令 | 说明 |
|------|------|
| **开启担保** | 初始化一张担保单，发填写模板 + 【担保单】草稿。 |
| **供方** @用户名 或 **供方：** @用户名 | 设置供方负责人。只发「供方」或「供方：」表示清空。 |
| **需方** @用户名 或 **需方：** @用户名 | 设置需方负责人。同上。 |
| **交易金额** 100u 或 **交易金额：** 100u | 设置交易金额。只发「交易金额」/「交易金额：」清空。 |
| **交易规则** xxxx 或 **交易规则：** xxxx | 从第一次出现「交易规则」起后面整段为规则；只发「交易规则」/「交易规则：」清空。 |
| **修改供方** @用户名 或 回复某人发 **修改供方** | 任意时刻修改当前担保单的供方；无 @ 用户名时可通过回复该用户设置（会记 user_id）。 |
| **修改需方** @用户名 或 回复某人发 **修改需方** | 同上，修改需方。 |
| （四项填完后出现）**供方确认** / **需方确认** 按钮 | 供方/需方各自点自己的按钮；两边都确认后发【担保单已确认】并标记完成。 |

---

## 四、供/需方/中介人员（与负责人区分）

| 命令 | 说明 |
|------|------|
| **设置供方人员** @用户1 @用户2 … 或 回复某人发 **设置供方人员** | 把用户加入「供方人员」列表；一条命令可同时 @ 多人。无 @ 也可用回复记昵称+user_id。 |
| **设置需方人员** @用户1 @用户2 … 或 回复某人发 **设置需方人员** | 同上，加入「需方人员」列表。 |
| **设置中介人员** @用户1 @用户2 … 或 回复某人发 **设置中介人员** | 同上，加入「中介人员」列表（无负责人，仅人员列表）。 |
| **查看成员** / **显示所有人** | 列出：供方负责人、供方人员；需方负责人、需方人员；中介人员。 |
| **删除供方人员** @用户名 或 回复某人发 **删除供方人员** | 从供方人员列表中移除该人。 |
| **删除需方人员** @用户名 或 回复某人发 **删除需方人员** | 从需方人员列表中移除该人。 |
| **删除中介人员** @用户名 或 回复某人发 **删除中介人员** | 从中介人员列表中移除该人。 |

---

## 五、置顶、链接、回收

| 命令 | 说明 |
|------|------|
| 回复某条消息后发 **置顶** | 把被回复的那条消息置顶。（公群/专群通用） |
| 回复已置顶消息后发 **取消置顶** | 取消该条消息的置顶。 |
| **临时链接** | 创建 24 小时有效的专群邀请链接（失败则用永久链接）；在「交易完成」或「回收群」时自动撤销。 |
| **回收群** | 从当前消息往上尽量清空聊天记录；踢出所有普通成员（ban+unban，保留群主/管理员/机器人）；撤销临时链接。 |

---

## 六、验群与上压（任何成员）

| 命令 | 说明 |
|------|------|
| **验群** | 在库则返回真专群信息（状态「交易完成」对外显示为「等待交易」）+ hy.mp4；不在库则「专群未备案」+ hy.mp4。 |
| **上压** | 发送上押说明图文（@oedb + 上押地址 + 上押规则）。 |

---

## 命令速记（专群内发）

```
授权专群
给我管理
修改群名 新名字

收到供方押金 1000
收到需方押金 500
清空供方押金 / 清空需方押金
开始交易
交易完成

开启担保
供方：@xxx  /  需方：@xxx  /  交易金额：100u  /  交易规则：...
修改供方 @xxx 或 回复某人发 修改供方
修改需方 @xxx 或 回复某人发 修改需方

设置供方人员 @xxx @yyy 或 回复发 设置供方人员
设置需方人员 @xxx @yyy 或 回复发 设置需方人员
设置中介人员 @xxx @yyy 或 回复发 设置中介人员
查看成员 / 显示所有人
删除供方人员 @xxx 或 回复发 删除供方人员
删除需方人员 @xxx 或 回复发 删除需方人员
删除中介人员 @xxx 或 回复发 删除中介人员

置顶 / 取消置顶（回复目标消息后发）
临时链接
回收群

验群
上压
```
